#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    // string str="om deshpande"; //dynamic in nature =>runtime resizing is possible
    // cout<<str<<endl;    
    // str = "omiii";
    // int len = str.length();
    // cout<<len<<endl;
    string str;
    getline(cin,str);
    cout<<str<<endl;    
    return 0;
}